---
const { prevBlogId, prevBlogTitle, nextBlogId, nextBlogTitle } = Astro.props;
---
<div class="flex justify-between py-8">
    {prevBlogId ?
      <a class="basis-1/2 text-left text-stone-600 hover:text-stone-800" href={`/blog/${prevBlogId}`}>
        <div class="text-base uppercase pb-2 tracking-wider font-bold">previous article</div>
        <div class="pr-3">{prevBlogTitle}</div></a> : <div></div>}
    {nextBlogId ?
      <a class="basis-1/2 text-right text-stone-600 hover:text-stone-800" href={`/blog/${nextBlogId}`}>
        <div class="text-base uppercase pb-2 tracking-wider font-bold">next article</div>
        <div class="pl-3">{nextBlogTitle}</div></a> : <div></div>}
</div>